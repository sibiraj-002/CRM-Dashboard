import { CreateBlogView } from "@/components/blogs";

export const metadata = {
    title: "Create Blog | Intelligence CRM",
    description: "Create a new blog post",
};

export default function CreateBlogPage() {
    return <CreateBlogView />;
}
