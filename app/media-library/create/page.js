import { CreateMediaView } from "@/components/media";

export const metadata = {
    title: "Add Media | Intelligence CRM",
    description: "Add media to the media library",
};

export default function CreateMediaPage() {
    return <CreateMediaView />;
}
