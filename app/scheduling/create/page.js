import { CreateScheduleView } from "@/components/scheduling";

export const metadata = {
    title: "Create Schedule | Intelligence CRM",
    description: "Schedule content publishing",
};

export default function CreateSchedulePage() {
    return <CreateScheduleView />;
}
